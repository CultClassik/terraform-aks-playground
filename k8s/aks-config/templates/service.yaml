apiVersion: v1
kind: Service
metadata:
  name: {{ include "aks-config.fullname" . }}
  labels:
    {{- include "aks-config.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aks-config.selectorLabels" . | nindent 4 }}
